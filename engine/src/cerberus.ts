import Pool from "commons/models/pool";

export default async (pool: Pool) : Promise<void> => {

    // buscar automações

    // testar condições

    // realizar swap

    // atualizar automação

    // registrar trade

}